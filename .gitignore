# Node.js and Package Manager specific
node_modules/
npm-debug.log*
yarn-debug.log*
.eslintcache

# Environment variables (CRITICAL for security)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build/Output directories
# Adjust these if your frontend build output is named differently (e.g., 'build' or 'dist')
build/ # Common for Create React App build output
dist/ # Common for other build outputs or compiled server code

# Logs and temporary files
logs/
*.log

# Editor/IDE specific files and directories
.vscode/
!.vscode/extensions.json # Keep this line if you want to share recommended VS Code extensions
.idea/ # Only if using JetBrains IDEs
.DS_Store # For macOS users
Thumbs.db # For Windows users